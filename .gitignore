# Build artifacts
*.exe
*.obj
*.lib
*.exp
*.pdb
build/

# Results / data / figures (keep local, per policy)
results/
*.json
*.csv
*.png
# the roofline figure is the deliverable -> committed (exception to *.png)
!docs/roofline.png

# Vendored deps (CUTLASS gets cloned for the FP4 DEEP DIVE)
third_party/
extern/
cutlass/

# Editor / OS
.vscode/
.idea/
Thumbs.db
